# Local data workspace

This directory is intentionally empty in source distributions. Dataset
download helpers may place lightweight manifests here, while Hugging Face
payloads remain in the configured external cache. Generated contents are not
part of the RewardHarness API and should not be committed.
